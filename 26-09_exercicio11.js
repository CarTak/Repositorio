/*
11. Leia um número binário e transforme-o em decimal
*/

const binario = prompt('Insira o numero binário para saber qual o valor em decimais')
let qtdItens = binario.length
let valorTotal = 0
for (let i = qtdItens; i == 0; i--) {
    const valor = binario[i]
    valorTotal=
}
